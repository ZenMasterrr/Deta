/// <reference types="react" />
interface IStrokeProps {
    text: string;
    maxFontSize?: number;
    shadow?: boolean;
    shadowColor?: string;
    fillColor?: string;
}
declare function Stroke({ text, maxFontSize, shadow, fillColor, shadowColor }: IStrokeProps): JSX.Element;
export default Stroke;
